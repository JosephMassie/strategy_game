// A file containing most generic game constants

export const CAMERA_SPEED = 40;

export const INPUT_TAP_COOLDOWN = 200;

// Building constants
export const BUILDING_COSTS = {
    mine: 20,
};

export const BUILDING_SPEED = 1000;
export const BUILDING_INCOME = {
    mine: 10,
};
